name: groups

clowder:
  clattner:
    - name: llvm/llvm-project
      groups: [notdefault]
    - apple/swift
    - name: tensorflow/tensorflow
      groups: [google]
  google:
    - name: git-repo
      path: repo
      source:
        url: gerrit.googlesource.com
        protocol: https
